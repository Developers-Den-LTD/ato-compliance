# Environment Configuration
NODE_ENV=development
PORT=3000

# Database Configuration
DATABASE_URL=postgresql://ato_user:ato_password@localhost:5432/ato_compliance
DB_HOST=localhost
DB_PORT=5432
DB_USER=ato_user
DB_PASSWORD=ato_password
DB_NAME=ato_compliance

# JWT Configuration (Generate your own with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-in-production-min-32-chars

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
